#!/usr/bin/env bash
# openclaw-gateway-restart — systemctl restart openclaw
set -euo pipefail

if [[ $# -ne 0 ]]; then
  echo "usage: openclaw-gateway-restart (no arguments)" >&2
  exit 64
fi

systemctl restart openclaw
systemctl status openclaw --no-pager --lines=5
